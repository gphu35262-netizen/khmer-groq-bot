services:
  - type: web
    name: telegram-premium-bot
    env: node
    plan: free
    buildCommand: npm ci
    startCommand: npm start
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: GROQ_API_KEY
        sync: false
      - key: NODE_VERSION
        value: 20.18.0
